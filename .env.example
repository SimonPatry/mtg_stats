# Mot de passe pour supprimer une partie (API /api/games/delete)
# En local : copie ce fichier en .env
# En prod Plesk : Domain → PHP → Environment variables → MTG_ADMIN_PASSWORD
# Fallback fichier : httpdocs/data/.mtg_admin_password (une ligne, le mot de passe)
MTG_ADMIN_PASSWORD=change-me
